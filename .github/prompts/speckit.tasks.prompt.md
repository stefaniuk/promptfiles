---
agent: speckit.tasks
---

$ARGUMENTS

Mandatory requirements:

- Define tasks using a strict TDD approach
- For each specified functionality, tasks must sequence as Red (write failing test first), Green (implement to pass), then Refactor (improve code without changing behavior)
- Ensure tests are always listed before implementation tasks
